services:
  cctv-helper:
    build: .
    container_name: cctv-helper
    restart: unless-stopped
    environment:
      - ADMIN_USER=admin
      - ADMIN_PASSWORD=YourStrongPassword
      - HTTP_PORT=3000
    ports:
      - "3000:3000"
    volumes:
      - cctv_data:/data

volumes:
  cctv_data: